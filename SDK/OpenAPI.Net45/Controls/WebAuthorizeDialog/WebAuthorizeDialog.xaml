<UserControl x:Class="KKBOX.OpenAPI.Controls.WebAuthorizeDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid x:Name="RootContainer">
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0.7" Background="Black" />
        <Grid x:Name="ContentContainer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10,0,25,0" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <WebBrowser Grid.Row="1" x:Name="WebViewControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />            
        </Grid>
    </Grid>
</UserControl>
